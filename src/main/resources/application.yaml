spring:
  application:
    name: projectmanagement

#  security:
#    user:
#      name: admin
#      password: admin123

  datasource:
    # URL includes compatibility flags for modern MySQL caching_sha2_password authentication
    url: jdbc:mysql://localhost:3306/projectcampDB?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: huzaifa#123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # Automatically updates database schema based on your Java entities
    show-sql: true     # Prints SQL queries to console (useful for development)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true



##  Security Configuration for JWT-based Authentication
#application:
#  security:
#    jwt:
#      # Generate a secure HS256 key (at least 256 bits/32 characters long)
#      secret-key: "317f82f4e31a5e664c8c65cd94d4e8e92ead51f1408992afc356f6e678018083"
#      access-token-expiration: 900000    # 15 minutes in milliseconds
#      refresh-token-expiration: 604800000 # 7 days in milliseconds

#  servlet:
#    multipart:
#      max-file-size: 10MB    # Handles task file attachments size threshold
#      max-request-size: 10MB